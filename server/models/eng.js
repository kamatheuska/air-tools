/* const mongoose = require('mongoose');

let PisosCTG = mongoose.model('PisosCTG', {
  address: {
    type: String,
    required: true,
    trim: true,
    minlenght: 5,
  },
  introductions: {
    host: {
      type: String,
    },
  },
});
*/
